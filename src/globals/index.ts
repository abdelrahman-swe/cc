import type { GlobalConfig } from 'payload'

import { HeroContent } from './HeroContent'
import { HomepageSettings } from './HomepageSettings'

export const globals: GlobalConfig[] = [HeroContent, HomepageSettings]
